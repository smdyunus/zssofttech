/**
 * Product toggles — flip to `true` when ready to expose in the UI again.
 * Routes under `/internships` and `/internship` remain buildable; only navigation and forms hide it.
 */
export const FEATURE_INTERNSHIPS = true;
